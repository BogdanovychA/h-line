# 📝 H-Line (Hotline App)

![Made in Ukraine](https://img.shields.io/badge/Made%20in-Ukraine-blue?logo=data%3Aimage%2Fsvg%2Bxml%3Bbase64%2CPHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSIxMjAwIiBoZWlnaHQ9IjgwMCI%2BCjxyZWN0IHdpZHRoPSIxMjAwIiBoZWlnaHQ9IjgwMCIgZmlsbD0iIzAwNTdCNyIvPgo8cmVjdCB3aWR0aD0iMTIwMCIgaGVpZ2h0PSI0MDAiIHk9IjQwMCIgZmlsbD0iI0ZGRDcwMCIvPgo8L3N2Zz4%3D)

[![DOI](https://zenodo.org/badge/1211130746.svg)](https://doi.org/10.5281/zenodo.19590690)
![GitHub License](https://img.shields.io/github/license/BogdanovychA/h-line?logoColor=grey&color=blue)

Застосунок для швидкої фіксації та надсилання звернень громадян через електронну пошту. Побудований на базі **Flet** (Python), підтримує генерацію документів (DOCX, MD, HTML) та автоматичне відправлення через SMTP.

## 🚀 Як запустити локально

Для роботи рекомендується використовувати менеджер пакетів [uv](https://github.com/astral-sh/uv).

### 1. Підготовка налаштувань
Створіть файл `.env` у папці `src/assets/` (можна скопіювати з прикладу):
```bash
cp src/assets/.env.example src/assets/.env
```
Відредагуйте `.env`, вказавши ваші налаштування SMTP (Gmail, Outlook тощо) та email отримувача.

### 2. Запуск через uv
Встановіть залежності та запустіть:

  ```bash
  uv sync
  ```
* **Як desktop-застосунок:**
  ```bash
  uv run flet run
  ```
* **Як вебзастосунок:**
  ```bash
  uv run flet run --web
  ```

---

## 🐳 Запуск через Docker

Це найкращий варіант для розгортання застосунку на сервері.

### 1. Підготовка та збірка
1.  **Налаштування версії:** Створіть файл `.env` у корені проєкту (можна скопіювати з прикладу):
    ```bash
    cp .env.example .env
    ```
    Вкажіть бажану версію образу в `APP_VERSION` (наприклад, `1.0.0`). Це дозволяє версіонувати образи та уникати конфліктів.
2.  **Запуск:** Використовуйте `docker-compose` для автоматичного налаштування:
    ```bash
    docker-compose up -d
    ```

### 2. Налаштування контейнера
* **Політика розгортання:** Використовується `pull_policy: missing`, що забезпечує стабільність — якщо образ із вказаною версією вже є локально, він не буде збиратися автоматично.
* **Порт:** Застосунок буде доступний за адресою `http://localhost:8081`.
* **Змінні оточення:** Docker використовує файл `src/assets/.env` для налаштувань самого застосунку.
* **Дані:**
    * Вихідні файли зберігаються в `./docker_data/output`.
    * Шаблони необхідно розмістити в `./docker_data/templates`.
    * Локалі (переклади) необхідно розмістити в `./docker_data/locales`.

---

## 🛠 Можливості та архітектура
Проєкт спроектований за принципами **SOLID** та **DRY**, що забезпечує високу гнучкість:

- ✅ **Локалізація (Fluent):** Повна підтримка багатомовності інтерфейсу, листів та логів через стандарт Project Fluent.
- ✅ **Генерація документів:** Автоматичне заповнення шаблонів звернень (DOCX, MD, HTML).
- ✅ **Гнучкий UI:** Підтримка розширених полів (категорія заявника, соціальний стан) згідно з офіційними класифікаторами.
- ✅ **SMTP інтеграція:** Підтримка SSL/TLS через абстрактні поштові протоколи.
- ✅ **Модульність (Registry Pattern):** Легке додавання нових форматів генерації або способів відправки без зміни ядра системи.
- ✅ **Аналітика:** Вбудована підтримка Google Analytics (Measurement API).
- ✅ **Безпека:** Чітке розділення конфігурації через змінні оточення.

---

## 👨‍💻 Для розробників (як розширити?)

Завдяки впровадженим патернам проектування, ви можете легко додавати новий функціонал:

1. **Новий формат генерації:** Створіть клас-спадкоємець `BaseTemplateManager` та зареєструйте його в `src/ui/main.py`:
   ```python
   GlobalGenerator.register(FileType.PDF, MyPdfManager)
   ```
2. **Новий поштовий протокол:** Створіть реалізацію `SMTPSenderBase` та зареєструйте її:
   ```python
   EmailManager.register(SMTPProtocol.CUSTOM, MyCustomSender)
   ```
3. **Додавання нової мови:** Створіть новий каталог в `src/assets/locales/` (наприклад, `en/`) та додайте файли `ui.ftl`, `mail.ftl` та `logs.ftl`. Додана мова підтягнеться автоматично.

---

## 📝 Ліцензія та авторство

Автор та власник майнових прав: **Андрій БОГДАНОВИЧ**.

Цей проєкт розповсюджується на умовах ліцензії **European Union Public Licence v1.2 (EUPL-1.2)**.
Ви можете використовувати, копіювати та модифікувати цей софт згідно з умовами ліцензії.

*Проєкт створено в межах особистої ініціативи з цифровізації державного сектору*
